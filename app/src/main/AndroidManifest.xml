<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.ubi.di.pdm.joselamarao.a2keyppasswords">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Inicio" android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" android:screenOrientation="portrait" />
        <activity android:name=".App" android:screenOrientation="portrait" />
        <activity android:name=".AddURL" android:screenOrientation="portrait" />
        <activity
            android:name=".GeneratePassword"
            android:theme="@style/AppTheme.generatePass"
            android:screenOrientation="portrait"></activity>

        <activity android:name=".seeEntry" android:screenOrientation="portrait">
        </activity>
    </application>

</manifest>